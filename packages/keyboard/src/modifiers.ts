export enum ModifierKey {
  Control = "Control",
  Shift = "Shift",
  Alt = "Alt",
  Meta = "Meta",
}

export const ctrl = ModifierKey.Control;

export const shift = ModifierKey.Shift;

export const alt = ModifierKey.Alt;

export const meta = ModifierKey.Meta;
