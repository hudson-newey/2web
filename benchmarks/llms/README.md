# 2Web LLM Performance Benchmarks

Benchmarks to evaluate the performance of various large language models (LLMs)
using the 2Web framework.

## Usage

You will need to install `web-codegen-scorer` globally.

```sh
$ npm install -g web-codegen-scorer
>
```
