# `site/`

This `site` package differs from the `content` package because where content is
concerned with a 1-n mapping of assets (1 source, many outputs), site files are
concerned with n-1 mappings (n source, 1 output).

This is useful for things like and sitemaps, robots.txt, SEO, and microdata.
